# Local Build Report

Last updated: 2026-06-19

## Completed

- Built a local Next.js website and app shell for Vercel deployment.
- Added source-backed brand styling using the confirmed V1 palette and approved PNG logo across the public website and internal software shell.
- Corrected the internal portal away from the old dark logistics-app styling to the brand guide's pure-white software canvas.
- Added public website routes:
  - `/website`
  - `/legal`
- Added app/runtime routes:
  - `/`
  - `/accountability`
  - `/booking`
  - `/journey`
  - `/tracking`
  - `/portal`
  - `/login`
- Added internal app routes:
  - `/admin`
  - `/driver`
- Added local runtime state so workflows can be tested before real Supabase credentials exist.
- Added a journey layer that maps the release-one lifecycle across Client, Admin, System, Driver, and Receiver roles as implementation source material.
- Added uploaded journey comparison evidence for Client Operational Contact, Client Billing Contact, Admin, and Driver journeys from `customer journey.zip`; this is retained as build documentation, not as a customer-facing workflow page.
- Added process accountability runtime for BOAS software actors, human gates, data objects, controls, and role access guardrails.
- Added in-app workflow rule notices for Admin, Driver, and system-level guardrails so blocked actions stay visible inside the software surface instead of relying on browser pop-up alerts.
- Renamed the active runtime's local guardrail notice helper to `showWorkflowNotice` and added source verification that rejects bare `alert(...)` calls, so workflow validation cannot quietly slide back into native browser popups.
- Replaced the local demo reset browser confirmation with an in-app confirmation panel (`Confirm Reset` / `Keep Demo Data`) and added source verification that blocks native `window.confirm`, `window.alert`, and `window.prompt` in the active runtime.
- Added route-aware software entry points: `/admin` preselects Admin login, `/driver` preselects Driver login, `/tracking` preselects Client Operational Contact with tracking context, `/booking` preselects Client Operational Contact with booking context, and `/portal` preselects Client Operational Contact with portal context. Authenticated Client routes can now open the matching client workspace view from route intent without changing the confirmed access model.
- Added `npm.cmd run verify:requirements`, a local source-backed runtime acceptance gate that checks product routes mount the software shell, confirmed actor workflows and guardrails remain present, Supabase/Vercel unknowns stay blocked, and local gaps remain explicit instead of being silently invented.
- Added `npm.cmd run verify:platform`, `.env.example` handoff placeholders, and `docs/platform-env-contract.md` to report the GitHub/Supabase/Vercel environment contract without creating projects or inventing credentials.
- Added `npm.cmd run verify:migrations`, a static Supabase migration guardrail verifier covering the source-backed base schema, APP-PRV-004 audit chain, BOAS Sheet 05 RLS draft, private POD storage, Policy #9 pricing, Policy #14 cancellation, Policy #8 failed delivery, Policy #15 goods acceptance, Policy #16 pickup standards, SOP-PUP-02 supplier-stop closeout, Policy #22 driver scheduling, Policy #27 WHS hazard controls, Policy #24 revenue reporting financial controls, Policy #20 AI use governance, Policy #6 NDB response controls, SOP-DEL-01 grouped delivery stops, SOP-DEL-05 delivery completion, Policy #18 disputes, Day 8 notices, SOP-EXC-03 unmatched billing accounts, and SOP-BIL-04 invoice approval. This does not replace live Supabase execution.
- Added `npm.cmd run verify:launch`, a read-only launch-readiness report for Git CLI, Supabase CLI, Vercel CLI, platform handoff values, environment pairing, and local Git repository initialisation.
- Added `npm.cmd run verify:local`, a single local preflight command that runs the source-backed requirement verifier, platform-contract report, launch-readiness report, Supabase migration guardrail verifier, Next route type generation, TypeScript, and a production build with isolated `NEXT_DIST_DIR=.next-preflight-build` output to avoid locked stale default build folders on Windows/OneDrive.
- Added `npm.cmd run verify:production`, a strict production readiness gate that runs the production platform and launch checks with `--strict`; it is expected to fail until the real GitHub, Supabase, Vercel, local Git, and live-tooling blockers are resolved.
- Removed the unused first-pass portal/workspace/provider scaffold and stale global CSS for those removed surfaces, so the build surface now has one active software runtime: `src/components/moto-co-logistics.tsx`. The requirement verifier now blocks those legacy scaffold files from returning.
- Added draft GitHub Actions workflow `.github/workflows/runtime-ci.yml` for the future connected repository. It runs dependency install, source-backed requirement verification, platform-contract reporting, Supabase migration guardrail verification, TypeScript, and the Next production build with a non-default build directory; it does not deploy or assume Supabase/Vercel credentials.
- Added a local-only `Reset Local Demo Data` login-screen command with in-app confirmation that clears the Moto & Co session-storage keys and reloads the seeded workflows, so fresh fixtures such as `MC-UNMATCHED-001` can be demonstrated without manual browser-storage editing.
- Added local role email plus generated testing-code login, session state, and access gates for app workspaces, with local expiry, one-use code consumption, attempt limits, request throttling, and generic unknown-email request behavior. Production Supabase Auth/email delivery remains a gap.
- Added release-one Supabase schema migration.
- Added Supabase audit hash-chain migration for APP-PRV-004.
- Added Supabase billing-notice migration for local Day 8 overdue, suspension, and reinstatement notice evidence records.
- Added Supabase account-level billing-notice follow-up migration so suspension and reinstatement notice evidence can be recorded without inventing a linked invoice.
- Added Supabase Day 8 overdue notice generation migration for local evidence records once an overdue invoice reaches due date plus 8 days, without claiming external notification delivery.
- Added Supabase unmatched billing account exception migration for SOP-EXC-03 account-match state, APP-ADM-005 queueing, and Admin-only correction to an active customer account before invoice inclusion.
- Added Supabase invoice approval gate migration for SOP-BIL-04 rendered-invoice approval evidence before dispatch/payment-monitoring states.
- Added Supabase invoice payment evidence migration for local paid-status evidence capture.
- Added Supabase access governance migration for local role assignment records and structured annual, role-change, departure, restore, revoke, or other review records.
- Added Supabase account suspension notice guardrail migration requiring matching Day 8 overdue notice evidence for non-payment suspension, Operational/Billing contact notification evidence for suspension, and both-contact notification evidence for reinstatement.
- Added Supabase supplier master-data monitoring migration for supplier-level review intervals and `SOP-MDM-01` review flags.
- Added Supabase first-login supplier setup migration for customer confirmation timestamps and supplier-link confirmation fields.
- Added Supabase customer activation eligibility migration for B2B/SEQ/physical-address review fields and no-PO-box delivery-address constraint.
- Added Supabase driver outcome capture migration for No Pickup, Bring Forward, and Failed Delivery evidence fields without finalising production reason codes.
- Added Supabase Policy #16 vendor pickup standards migration for ready-by-10:00am, labelled, packaged, grace minutes, No Pickup category, compliance note, and pickup standards policy reference fields; later extended the No Pickup reason constraint for SOP-PUP-03 supplier refusal and wrong-items reasons.
- Added Supabase Policy #15 goods acceptance migration for item-specific acceptance confirmation, final dock-decision/no-under-protest evidence, refused-goods evidence, and no-billable-row guardrails.
- Added Supabase SOP-PUP-02 supplier-stop closeout migration for correct-dock, customer-list-review, no-ad-hoc-record, dock-contact, leave-dock, closeout timestamp, outcome summary, and delivered-status guardrails.
- Added Supabase SOP-DEL-05 delivery completion migration so `delivery_proof` insert system-completes the delivery, marks it billing-ready, updates the linked pickup request, and blocks direct Delivered status writes outside the proof-completion trigger path.
- Added Supabase SOP-DEL-01 delivery stop grouping migration so a grouped delivery stop can cover multiple work items for the same account/address and one proof can system-complete every delivery in that group.
- Added Supabase invoice dispatch record migration for local invoice dispatch evidence without claiming production PDF/email delivery.
- Added Supabase Policy #24 revenue reporting financial controls migration for Admin-only month-end financial reconciliation records, invoice links, no-off-system-revenue confirmation, 5-business-day reconciliation due dates, PII audit trigger, and 7-year retention queueing.
- Superseded/extracted: the Policy #25/#26 conditional activation gate migration was removed from the active logistics migration set and preserved under `hcm-extract/supabase-migrations/` for future HCM reuse.
- Added Supabase Policy #20 AI use governance migration for Admin-only draft review records, approved agent IDs, Admin review evidence, no-autonomous-send, no-batch-approval, and no-commercial-decision constraints without claiming a live AI provider.
- Added Supabase Policy #6 NDB response migration for Admin-only suspected-incident records, containment evidence, APP-PRV-004 audit refs, 30-day assessment deadline, Privacy Owner-only decision guardrails, eligible-breach notification evidence, and 7-year post-breach review report retention.
- Added Supabase operational-notice migration for local customer update/outbox records without claiming production email/SMS/in-app delivery.
- Added Supabase operational-notice type-alignment migration for account suspension, account reinstatement, and billing-query acknowledgement outbox records.
- Added Supabase notification-failure exception routing migration so operational or billing notice rows explicitly marked `failed` queue APP-ADM-005 exceptions without treating `provider_not_configured` as a failed delivery.
- Added Supabase pricing master-data monitoring migration for `Policy #9` / `SOP-MDM-02` review flags.
- Added Supabase account-activation notice migration so account-level activation updates can be stored without an order reference.
- Added Supabase run compilation sequencing migration for APP-ADM-002 run brief order, pickup request assignment fields, and local fleet-compliance attestation.
- Added Supabase first-pass role/access RLS migration from BOAS Sheet 05 for Client Operational Contact, Client Billing Contact, Driver, Admin, and Receiver no-login boundaries.
- Added Supabase fleet vehicle register migration for BOAS Sheet 06 / APP-FLT-001, including Admin-managed vehicle records, assigned-driver visibility, and run assignment guardrails for current registration/insurance and no open defect.
- Added Supabase delivery-proof storage contract migration for private `delivery-proof` object paths, assigned-driver/Admin upload policy, linked-role read policy, proof path enforcement, and automatic Policy #5 retention queue rows.
- Added Supabase pricing change-log guardrail migration so `price_rules` rows require pricing master-data change-log reason, Owner approval evidence, and approved/executed status before taking effect.
- Added Supabase run-day cadence guardrail migration so future pickup requests and compiled runs must resolve to the confirmed Tuesday/Thursday release-one cadence.
- Added Supabase pickup request retention queue migration so `pickup_requests` rows queue Policy #5 7-year retention review from actual run date.
- Added Supabase supplier/master-data retention queue migration so closed supplier actor records queue relationship-end + 7-year review and `master_data_changes` rows queue change-date + 7-year review.
- Added Supabase Policy #27 WHS hazard migration for supplier-premises WHS No Pickup stops, unresolved hazard status, source reference, guardrail checks, and Admin follow-up indexing.
- Added browser Supabase environment guardrails so local or preview app builds refuse to create a client when `NEXT_PUBLIC_SUPABASE_ENV=production`, enforcing the confirmed no-preview-to-production Supabase rule without inventing project credentials.
- Added CAP-MCL-002 Run Planning Monitor to Admin Dispatch, with night-before compile status, named driver/vehicle assignment rate, fleet gate pass rate, Admin intervention count, and APP-ADM-002 exception queueing without claiming live pg_cron automation.
- Added CAP-MCL-001 / POL-MCL-001-001 Supplier Approval Gate to Admin Suppliers, with dock-access, packaging-standards, pickup-window, and written-evidence controls before an Active supplier can be saved or reactivated.
- Added seed SQL for Policy #9 pricing rules.
- Added `.env.example`.
- Added source-backed release-one implementation rules.

## Implemented Workflows

- Customer registration with required business, operational contact, billing contact, delivery address, approved supplier, and Collection Notice acknowledgement.
- Customer registration and Admin-created Workshop CRM records reject PO box delivery addresses.
- Customer registration no longer captures a local password; local access uses role email plus testing-code entry.
- Admin-controlled Workshop CRM list with New Workshop and CRM Review actions for actor details, Operational/Billing contacts, internal owner, relationship tier/status/risk, review date, next action, supplier links, relationship events, event-level next actions, and obligations.
- Added a source-backed `CRM Rhythm Monitor` for the lean Village release-one subset so Admin can see incomplete workshop CRM records, due reviews, due next actions, overdue/disputed obligations, at-risk relationship records, and open issue visibility without implementing deferred relationship health scoring.
- Admin access governance with local role assignment visibility, staff annual-review due indicators, Receiver no-login model, structured access review types (`annual`, `role_change`, `departure`, `restore`, `revoke`, `other`), access review records, revoke/restore controls, and login blocking for revoked local roles.
- Pending account creation and Admin activation.
- Admin activation now opens an eligibility review checklist for B2B workshop fit, physical non-PO-box delivery address in the SEQ service area, Operational/Billing contacts, and approved supplier before courier eligibility is switched on.
- Pending client workspace that shows submitted account details and blocks booking actions until Admin activation.
- Client first-login supplier setup gate after activation: Client Operational Contact confirms approved supplier links before the normal booking portal opens, or sends a supplier review request into the Admin exception queue.
- Admin-controlled supplier add/remove flow.
- APP-PRV-004 local audit trail with PII-action flagging, protected-object classification, sequence numbers, previous/current event hashes, chain verification, and visible chain-break status.
- Pickup request submission using account-linked suppliers.
- 12:30pm Brisbane cut-off handling now resolves adjusted requests to the next available Tuesday/Thursday run date and records whether the change was caused by cut-off, a past date, or a non-run-day request.
- Account suspension behavior represented at the data level.
- Structured price rule table management with tyre count, weight band, rate mode, effective date, required change reason, Owner approval reference, and local master-data change rows.
- Exception queue with daily APP-ADM-005 structured Admin alert, local review acknowledgement, and Admin investigation/close workflow using linked invoice, work item, POD proof context, or supplier master-data record context.
- Driver run brief with pre-trip/run-brief issue reporting and missing-stop exception routing to Admin.
- Admin APP-ADM-002 local run compiler groups eligible work by supplier and delivery geography, requires named driver and an Admin-managed vehicle record, checks local registration/insurance/defect evidence from the fleet register, and writes driver-visible sequence fields.
- Admin fleet asset register with vehicle name/call sign, registration plate, assigned driver, status, registration expiry, insurance policy/reference, insurance expiry, GVM, service dates, defect status, ownership type, make/model/year, last reviewed date, notes, required change reason, APP-PRV-004 audit, and local master-data change rows.
- Admin Driver Directory with logistics-facing driver account details, active/inactive status, review date, notes, required change reason, APP-PRV-004 audit, and local driver master-data change rows. HCM-owned legal classification, agreements, verification evidence, disciplinary/removal consequences, and expansion employment/payment models were extracted from the active logistics app.
- Admin run-close review shows local driver close records, open-stop check, delivered counts, linked POD proof count, linked open exceptions, and unresolved production accounting handoff status.
- Admin Policy #22 manual driver availability records with note, notice received date, calculated due date, late-notice flag, contingency evidence, and unavailable/leave guard blocking dispatch assignment for affected run dates.
- Supplier-grouped Driver pickup stops with pickup item capture from active price rules, calculated pickup totals, structured No Pickup evidence capture, SOP-PUP-02 supplier-stop closeout evidence, and SOP-RUN-04 future-pickup bring-forward evidence capture.
- SOP-PUP-02 / Policy #16 / SOP-PUP-03 Driver pickup standards workflow with ready-by-10:00am, labelling/mismatch, packaging, supplier-refusal, wrong-items, grace-minute, compliance-note evidence, structured No Pickup category capture, no-billable-row evidence, supplier-stop closeout, and Admin supplier pickup standards monitoring/investigation.
- Policy #15 Driver goods-acceptance workflow with item-specific acceptance standards for tyres, batteries, parts/accessories, final dock-decision/no-under-protest confirmation, refused-goods No Pickup evidence, and no-billable-row evidence.
- Policy #27 Driver WHS workflow with fatigue/health and WHS incident run-issue reporting, WHS hazard at supplier premises as a No Pickup category, non-billable hazard stop evidence, APP-ADM-005 `WHS Hazard` exception routing, Admin supplier follow-up prompt, and unresolved-return block wording.
- Superseded/extracted: Policy #19 driver conduct/disciplinary material is preserved under `hcm-extract/` and is not an active logistics workflow.
- Policy #20 Admin AI draft review workflow with flagged CRM, supplier, and exception candidates, local CTA draft creation, required Admin review notes before approval/rejection, `Approved - Not Sent` status, duplicate unread draft blocking, APP-PRV-004 audit rows, and explicit live-provider/send-path blockers.
- Policy #6 Admin NDB response workflow with suspected incident intake, awareness date, personal-information detail, affected estimate, containment evidence, APP-PRV-004 audit refs, system/Digiverse evidence fields, 30-day assessment due date, Privacy Owner unnamed blocker, no Admin/automation eligible-breach decision, and post-breach review report retention when a report exists.
- SOP-DEL-01 Driver delivery stop grouping by client account and delivery address, with grouped work-item review, read-only pickup-captured pricing, one receiver name/signature per delivery location, grouped proof completion, and grouped failed-delivery evidence fan-out to each work item.
- Delivery proof capture with receiver name, signature, local delivery ID, private proof object path, and 7-year retain-until date.
- Delivered status is now system-written after delivery proof capture under SOP-DEL-05; Driver captures POD, the local system marks Delivered/billing-ready, and Admin direct Delivered status shortcuts are blocked.
- Admin retention register showing pickup request records retained for 7 years from run date, supplier records retained for supplier relationship + 7 years, master-data change logs retained for 7 years from change date, POD proof records retained for 7 years from delivery date, private `delivery-proof/deliveries/{delivery_id}/signature.png` paths, and destruction blocked pending Privacy Owner approval.
- Admin billing review, SOP-EXC-03 unmatched billing account queue, billing-candidate exclusion, account-correction modal with required Admin investigation note, local draft invoice batches, SOP-BIL-04 rendered-invoice approval before dispatch, local inline invoice preview, local invoice dispatch evidence capture for unpaid invoices, client invoice visibility, Client Billing Contact invoice visibility, payment evidence capture after dispatch before Paid status, Policy #24 month-end financial reconciliation with no-off-system-revenue confirmation and open accountant/Otimi reporting gaps, billing-query escalation to Admin, local billing-query acknowledgement records, and local billing-query status/investigation visibility.
- Fresh local seed data now includes `MC-UNMATCHED-001`, a proof-linked Delivered SOP-EXC-03 verification fixture with intentionally unknown `account_id`, receiver/signature proof `proof-seed-unmatched-1`, and billing-ready evidence. On fresh storage it should appear in the Unmatched Billing Account Queue and stay excluded from invoice groups until Admin records an account correction note. Use the local-only `Reset Local Demo Data` login-screen command to reload fresh seeded workflows in the current browser tab.
- Admin Day 8 overdue notice queue, system-generated local notice evidence, and Admin fallback notice records, with non-payment suspension gated until the overdue notice exists.
- Billing Contact portal with inline invoice preview, invoice evidence, local dispatch record visibility, account notices, billing updates, local Day 8 overdue notice visibility with generation-source labels, local suspension/reinstatement notice visibility, local payment evidence visibility, billing-query status/investigation visibility, and billing-query escalation, without pickup or supplier setup controls.
- Admin overdue account detail, account-name confirmation guard for suspension, Operational/Billing contact notification evidence, and reinstatement evidence capture.
- Client Operational Contact tracking workspace is account-scoped, searchable by order/con note/supplier/status, reads pickup, delivery, POD proof, retention, and linked exception records, and can raise a delivery dispute from the tracked order.
- Client and Admin operational update/outbox records now exist locally for account activation, account suspension/reinstatement, pickup request submission, 12:30pm cut-off schedule adjustment, driver pickup confirmation, out-for-delivery status, delivery outcomes, supplier setup requests, delivery dispute acknowledgement, and billing-query acknowledgement. Records stay `local_record_only` with `provider_not_configured` until the production notification provider/channel is confirmed.
- Admin suspension/reinstatement now creates local account-level billing notice records for Client Billing visibility and local operational update records for Client Operational visibility, without claiming external delivery.
- Failed operational or billing notice rows now route into the Admin APP-ADM-005 queue locally and in Supabase draft triggers; `provider_not_configured` remains a production delivery-provider gap rather than a failure.
- Product route `/journey` now mounts the active software shell; uploaded journeys remain implementation source material and acceptance evidence, not a journey-map product page.
- Accountability page maps software actors such as `APP-ADM-001`, `APP-DRV-002`, `APP-ADM-004`, and `APP-PRV-004` to accountable process tasks, human gates, protected data objects, and role guardrails.
- Login page provides generated local code-entry sessions for Client Operational Contact, Client Billing Contact, Admin, and Driver.
- Login page checks the local access register before issuing/verifying the testing code, expires codes locally after 5 minutes, consumes valid codes after one use, limits local verification attempts to 3, and throttles local role/email code requests to 5 per 10 minutes.
- Admin, Driver, Portal, Tracking, Accountability, and Journey workspaces are role-gated.
- Booking registration remains public, but pickup request submission is locked to Client Operational Contact.
- Product routes now steer the runtime to the accountable actor and workflow context instead of opening a generic login surface.
- Admin and Driver validation guardrails now show dismissible in-app workflow-rule notices for missing evidence, blocked dispatch, blocked fleet changes, POD requirements, run-close blocks, supplier/pricing governance requirements, access-governance evidence, invoice/payment evidence, suspension/reinstatement evidence, and Day 8 overdue notice prerequisites.
- Supabase RLS/storage migrations now map BOAS Sheet 05 role boundaries into database policies: account-scoped Client Operational access, billing-only Client Billing access, assigned Driver access, Admin full-platform access, Admin-only pricing/master-data writes, suspended-account pickup blocking, private proof-object upload/read controls, automatic proof retention queueing, and Receiver no-login.
- Admin supplier master data captures dock contact role, named dock contact gap status, pickup window, packaging notes, CAP-MCL-001 approval-gate evidence, status, last-reviewed date, supplier-level review interval, inline supplier edits, structured review/archive/reactivation reasons, structured local master-data change rows, incomplete/stale supplier review flags, and archive blocked while open work references the supplier.
- Admin pricing master data captures structured `price_rules` rows, requires written reason plus Owner approval reference before local create/edit/archive/reactivation changes take effect, and monitors incomplete or unlogged pricing rows for APP-ADM-005 exception routing.
- Supabase pricing guardrails now require pricing `master_data_changes` evidence before `price_rules` can take effect; seed pricing includes matching initial Policy #9 / SOP-MDM-02 change-log rows.
- Supabase run cadence guardrails now reject future pickup request/run records where `actual_run_date` or `runs.run_date` is not Tuesday/Thursday, and require `schedule_adjusted` evidence whenever the actual run date differs from the requested date.
- Supabase pickup request retention queueing now records `pickup_requests` retention review rows from the actual run date.
- Supabase supplier/master-data retention queueing now records closed supplier actor retention rows from relationship end and master-data change retention rows from logged change date.
- Supabase notification-failure triggers now create Admin exceptions when operational or billing notice delivery status is explicitly `failed`.
- Supabase account-level billing notices now allow invoice-less suspension/reinstatement records while preserving invoice-linked Day 8 notice uniqueness.
- Supabase Day 8 overdue notice generation now creates local evidence records for due overdue invoices through a callable generator and invoice-change trigger. Production daily scheduling remains open until the live platform is connected.
- Supabase unmatched billing account exception handling now records billing account match status on pickup requests, queues APP-ADM-005 exceptions for delivered uninvoiced work with inactive/invalid/unmatched accounts, and provides an Admin-only correction function that blocks already-invoiced retro-modification.
- Supabase operational notice constraints now include local account suspension, account reinstatement, and billing-query acknowledgement record types used by the runtime.
- Supabase Policy #24 financial reconciliation records now represent the source-backed month-end revenue control, while external accountant/BAS handoff and Otimi Rules reporting cadence remain open.
- Superseded/extracted: Policy #25/#26 conditional activation review records were moved out of the active logistics migration set and preserved for future HCM reuse.
- Supabase browser-client creation now validates `NEXT_PUBLIC_APP_ENV` against `NEXT_PUBLIC_SUPABASE_ENV` and blocks local/preview app builds from connecting to a production-labelled Supabase backend.
- Driver pre-trip/run-brief issues and missing-stop reports create Admin-visible exception queue items; open pre-trip/run-brief issues block departure locally until Admin closes them.
- Driver No Pickup and Failed Delivery outcomes now use structured local modals with required reason, optional handling note, and Admin exception routing instead of browser prompts. Bring Forward now uses the SOP-RUN-04 pattern: future pickup, same supplier already on route, no unscheduled detour, item/quantity capture, and original intended run date retained.
- Driver pickup confirmation now requires item band/quantity capture from `price_rules`; delivery cannot start until all supplier pickup stops are resolved.
- Suspended clients can still log in but pickup submission is blocked; Admin suspension records capture date, reason, and Operational/Billing contact notification status.
- Account suspension and reinstatement records now produce local account notices labelled by notice type instead of displaying every billing notice as a Day 8 overdue notice.
- Legal page lists required legal surfaces without publishing draft legal copy.

## Verification

Commands run:

- `npm.cmd install`
- `npm.cmd run verify:local`
- `npm.cmd run verify:launch`
- `npm.cmd run verify:production` (expected failure while production blockers remain open)
- `npm.cmd run typecheck`
- `npm.cmd run verify:requirements`
- `npm.cmd run verify:platform`
- `npm.cmd run verify:migrations`
- `npm.cmd run build`
- `npm.cmd audit --json`

Draft CI gate:

- `.github/workflows/runtime-ci.yml` mirrors the local source-backed gates with `npm ci`, `npm run verify:requirements`, `npm run verify:platform`, `npm run verify:migrations`, `npm run typecheck`, and `npm run build` using `NEXT_DIST_DIR=.next-ci`. The local one-command version is `npm.cmd run verify:local`.

Browser checks:

- Desktop route checks for `/`, `/accountability`, `/booking`, `/portal`, `/admin`, `/driver`, `/tracking`, `/login`, and `/legal`.
- Desktop route check for `/journey` after the single-runtime cleanup verified that it opens the active software shell rather than a journey-map display page.
- Mobile viewport checks for `/`, `/booking`, `/admin`, and `/driver`.
- Mobile viewport check for `/journey` after the single-runtime cleanup verified the active software shell remains accessible.
- Historical uploaded-journey comparison evidence remains in `docs/customer-journey-comparison.md`; the product route no longer presents that comparison as the app experience.
- Accountability page verified with 12 software actors, 12 accountable workflow tasks, 9 protected data objects, and 6 role access guardrail cards.
- Local code login flow verified for Admin, Client Operational Contact, Client Billing Contact, and Driver.
- Local hardened code-login behavior verified in browser on fresh `localhost` state: unknown Admin email request entered the same code-entry flow as a valid request, correct fake-code verification consumed the request and returned a generic failure, a valid Admin request showed a generated code, one wrong code showed `Incorrect login code. 2 attempt(s) left.`, the correct code opened the Admin operations shell, and the sixth code request for one role/email inside the local window was blocked.
- Pending registration workflow verified: submitted account lands in pending activation workspace with booking actions hidden.
- Customer eligibility workflow verified in browser: registration with `PO Box 12 Southport QLD` was blocked; the same account with `18 test street southport qld` submitted to Pending; Admin opened `Review Eligibility`, saw B2B/SEQ physical-address/contact/supplier checks with `Activate Account` disabled; after checking all four, the account became Active and showed `Activation eligibility: Reviewed 18 June 2026; Physical address recorded`.
- Client first-login supplier setup verified in browser: `gc@example.com` logged in with a generated local code, landed on `SUPPLIER SETUP` instead of `My Deliveries`, saw approved suppliers preselected, confirmed supplier access, then opened the Client Portal with `+ New Pickup` available and Profile showing `Supplier setup: Confirmed 18/06/2026`.
- Admin can access Admin console and is denied from Driver.
- Driver can access Driver workspace and is denied from Accountability.
- Client Operational Contact can submit pickup requests; Admin cannot submit client pickup requests.
- Focused journey slices verified for `/booking`, `/admin`, `/driver`, `/tracking`, and `/portal`.
- Booking page journey slice verified with registration, activation, pickup request, and cut-off stages.
- Previous journey-comparison layout checks are superseded by the single-runtime route checks; wide source comparison tables now belong in documentation, not the foreground software route.
- Registration workflow created a pending account.
- Pickup request workflow created a request and adjusted the run date after cut-off.
- Run-date cadence verified: the shared date-rule module keeps a Thursday request before 12:30pm on Thursday, moves a Thursday request at 12:30pm Brisbane time to Tuesday 23 June 2026, and moves a future Monday request to Tuesday 23 June 2026 without marking cut-off. Browser verification through the Client Operational Contact portal created `CADENCE-001` after cut-off and showed run `23 June`.
- POD workflow blocked Delivered without signature.
- POD workflow accepted receiver name plus signature and set Delivered.
- Admin billing workflow created a local draft invoice from delivered unbilled work.
- Client billing workflow displayed the draft invoice with line items and proof status.
- Local invoice preview verified in browser: Admin Billing opened `Invoice Preview - INV-SEED-001` with Gold Coast Cycles, billing email, issue/due dates, proof `proof-seed-1`, line `MC-003 / A1 Accessories / A1-3311`, subtotal `$35.00`, GST `$3.50`, and total `$38.50`; Billing Contact opened the same preview and used the preview's `Raise Billing Query` action to open `Billing Query - INV-SEED-001`.
- Client billing query workflow created a Billing Dispute in the Admin exception queue.
- Admin dispute investigation workflow verified in browser: the Billing Dispute for `INV-740800` showed linked invoice `INV-740800`, linked delivered work `MC-003`, receiver `Jake T`, and proof record `proof-seed-1`; Admin closed it with a Policy #18 / APP-DRV-003 investigation note and the close was retained in the exception card plus audit trail.
- Daily structured exception alert verified in browser: Admin Dashboard showed the APP-ADM-005 alert with date, open exception count, category count, oldest age, linked work/POD proof counts, and owner; `Open Exception Queue` navigated to the queue; `Mark Alert Reviewed` stored the review state and wrote `Daily exception alert reviewed` to the audit trail.
- Admin Policy #5 retention workflow verified in browser on fresh `localhost` storage: Admin updated A1 Accessories supplier master data with a required change reason, then Retention showed `Policy #5 Retention Queue`, 2 POD records, 4 pickup requests, 6 supplier records, 2 generated change logs, 0 due reviews, and 2 private-bucket proof records. The register showed pickup rows `MC-003`, `MC-UNMATCHED-001`, `MC-001`, and `MC-002` retained from run date to May 2033; active supplier rows with `Retention starts at archive/closure`; generated `A1 Accessories - dock_contact_role` and `A1 Accessories - review_interval_days` change logs retained until `18 June 2033`; POD records `proof-seed-1` and `proof-seed-unmatched-1` retained until `13 May 2033`; private `delivery-proof/deliveries/{delivery_id}/signature.png` paths; and disabled destruction actions pending Privacy Owner approval. Audit also showed `Supplier updated`, `Hash Verified`, and the master-data old/new values.
- APP-PRV-004 audit workflow verified in browser: Admin Audit tab showed Tamper-Evident Chain, `Hash Verified`, 25 audit events, 25 PII actions, 0 chain breaks, protected-object labels, sequence numbers, previous/current local hashes, and `local-fnv1a-v1` algorithm labels.
- Workshop CRM workflow verified in browser: Admin created `harbour crm workshop` from `New Workshop`, recorded Operational and Billing contacts, owner `admin`, supplier link `Link International`, next action `confirm supplier access`, relationship event `Decision - admin created controlled workshop record`, obligation `confirm account access due TBD`, and Pending status; APP-PRV-004 audit trail recorded `Customer account created` for the CRM action with verified hash chain.
- CRM Rhythm Monitor and CRM Review workflow verified in browser on fresh `localhost` storage after the Village v2.0 source re-check: Admin Clients showed `CRM Rhythm Monitor`, `Rhythm Flags`, `Reviews Due`, `Actions Due`, `Obligations Due`, and 2 incomplete workshop records. Opening `CRM Review - Gold Coast Cycles` confirmed the lean Village subset for actor/contact details, relationship owner/tier/status/risk, review date, next action, supplier links, events with event-level next action owner/due date, and obligations. Blank save was blocked by `Village CRM requires a named internal relationship owner.`; event follow-up without event description was blocked by `Event next action requires an event description.`; an incomplete obligation was blocked by `Obligations require title, description, and risk if breached.`. Completing owner `Admin CRM Owner`, review date `15 July`, next action due `1 July`, event `Browser CRM review recorded from Village RM10 operating rhythm.`, event next action `Schedule first RM10 review call` due `8 July`, and obligation `Confirm account access cadence` due `10 July` saved the record. The monitor dropped to 1 rhythm flag / 1 incomplete record, Gold Coast Cycles no longer showed CRM incomplete flags, and the card displayed owner, review date, next action due date, open obligation due date, and latest CRM event. APP-PRV-004 Audit showed `Hash Verified`, 4 audit events, 0 chain breaks, and a verified `Customer account updated` row with `Gold Coast Cycles CRM record reviewed; owner Admin CRM Owner; relationship Active; event added; obligation added; 2 supplier link(s)`. A runtime bug found during this verification was patched so CRM review, event next-action, and obligation due-date inputs persist date values through local save. This remains rhythm/action/obligation visibility, not deferred relationship health scoring.
- Admin client supplier-access governance hardened and verified in browser: Admin Clients now opens a controlled `Add/Remove Supplier Access` modal from customer supplier pills instead of changing access immediately, and CRM Review requires `Supplier Access Change Reason` when supplier links are edited there. Fresh browser verification removed `A1 Accessories` from Gold Coast Cycles, confirmed blank save is blocked with `Supplier access change reason required.`, recorded `browser supplier access removal evidence`, saw the customer card change to `Supplier access: Link International`, saw `Latest supplier access change: remove A1 Accessories - browser supplier access removal evidence`, and confirmed APP-PRV-004 Audit showed a verified `Customer account updated` row with the same reason/evidence.
- Policy #22 driver availability workflow verified in browser after patching the notice-date input event path: Admin recorded Damo Reeves as `unavailable` for `14 May 2026` with note `policy 22 unavailable run date`, notice received `14 May 2026`, due date `13 May 2026`, late-notice flag, and contingency `admin to hold run pending driver cover and notify affected clients`; Dispatch showed `MC-002` as an unassigned stop for `14 May`, displayed the availability block with notice/due/late/contingency evidence, and disabled the assignment button as `Driver Unavailable`. The browser was reset back to clean seeded `/booking`.
- Policy #27 WHS hazard workflow verified in browser on `localhost`: Admin assigned `MC-002` to Damo, Driver completed pre-trip checks, opened `No Pickup`, selected `WHS hazard at supplier premises`, recorded reason `unsafe forklift movement at supplier dock entry` and handling note `driver stayed outside hazardous area and called Admin`, and saved the outcome. Admin APP-ADM-005 then showed a `WHS Hazard` exception with Policy #27 supplier follow-up and driver-safety wording, Admin Suppliers showed the Policy #27 WHS monitor with one WHS hazard stop, and the browser was reset back to clean seeded `/booking`.
- Driver pickup workflow recorded Picked Up and moved the stop from Pickup into Delivery with Start Delivery available.
- Supplier-grouped pickup workflow verified in browser: Client created pickup `MC-37776`, Admin dispatched it to Damo with vehicle `MCO-VERIFY`, Driver saw it under supplier stop Link International, captured `4 or more tyres` quantity 4 from `price_rules`, calculated `$48.00`, confirmed pickup, and the stop moved into Delivery.
- Admin supplier archive/reactivation workflow verified in browser on fresh `127.0.0.1` storage: Admin Suppliers showed Link International and Ficeda with `Open work: 1` and disabled `Archive` buttons, while A1 Accessories showed `Open work: 0` and an enabled archive path. The `Archive Supplier - A1 Accessories` modal required `Reason / Evidence`; blank confirmation was blocked with `Supplier action reason required.`; entering `Browser supplier archive verification - no open work references A1 Accessories.` moved A1 to `ARCHIVED` with `Reactivate` available. The `Reactivate Supplier - A1 Accessories` modal also blocked blank evidence, accepted `Browser supplier reactivation verification - controlled supplier restored.`, and returned A1 to active/review state with `Archive` available again. Audit showed `Hash Verified`, 5 audit events, 0 chain breaks, verified `Supplier archived` and `Supplier updated` APP-PRV-004 rows, plus master-data change rows for `status`, `archived_at`, `last_reviewed`, and `reactivated_at` with the Admin reasons retained.
- Admin pricing workflow verified in browser: structured `price_rules` form renders service variant, item type, tyre count, weight band, rate mode, effective dates, required change reason, and Owner approval reference; edit mode populates a current rule.
- Admin pricing archive/reactivation workflow verified in browser on fresh `127.0.0.1` storage: Admin Pricing showed 8 active Policy #9 rules and 0 pricing governance flags; the seeded `1 tyre` rule opened `Archive Pricing Rule - 1 tyre` with required reason and Owner approval reference fields. Blank confirmation was blocked with `Pricing action reason required.`; entering only a reason was blocked with `Owner approval reference required before a local price rule can be changed.`; entering `Browser pricing archive verification - temporary retirement of seeded 1 tyre tier.` plus `OWNER-PRICE-BROWSER-ARCHIVE-001` moved the rule to `ARCHIVED`, exposed `Reactivate`, and generated a `local-price-change-*` change-log ID. Reactivation also blocked blank evidence, then accepted `Browser pricing reactivation verification - restore seeded 1 tyre tier after archive test.` plus `OWNER-PRICE-BROWSER-REACTIVATE-002`, returned the rule to `ACTIVE`, and generated a new `local-price-change-*` ID. APP-PRV-004 Audit showed `Hash Verified`, 5 audit events, 0 chain breaks, verified `Pricing rule updated` rows, and pricing master-data rows for `status`, `change_log_id`, and `owner_approval_ref` with reasons, Owner approval references, and effective date retained.
- Driver delivery sign-off verified in browser: price selections render from structured rules and do not expose manual price entry.
- Driver pre-trip defect workflow verified in browser: Driver submits a `Pre-Trip Defect`, departure is blocked by the open issue, and Admin sees the run-level exception in the dashboard queue.
- Admin overdue/suspension/reinstatement workflow verified in browser: Admin marked `INV-740800` overdue, suspended Gold Coast Cycles with exact account-name confirmation and Operational/Billing notification evidence, Client login was still allowed but `+ New Pickup` was blocked while existing and in-transit work remained visible, Admin marked the invoice paid, reinstated the account with payment evidence and both-contact notification evidence, and Client booking access was restored.
- Client Billing Contact workflow verified in browser: `accounts@goldcoastcycles.example` logged into a billing-only portal, saw `INV-740800`, proof line evidence, account suspension/reinstatement notices, and `Raise Billing Query`; operational booking controls such as `+ New Pickup`, `My Orders`, and `Suppliers` were absent; the submitted billing query appeared in the Admin exception queue.
- Day 8 overdue notice workflow verified in browser on clean `localhost` storage: Admin Billing showed seeded overdue invoice `INV-SEED-001`, notice due `29 May 2026`, `Record Day 8 Notice`, and disabled `Notice Required` suspension; after recording, the queue dropped to 0, the invoice showed `Day 8 overdue notice recorded 18 June 2026; channel local record only`, `Suspend Account` became available, Billing Contact and Client Operational Contact billing views showed the notice, and Admin Audit recorded `Day 8 overdue notice recorded` with `Hash Verified`.
- Local payment evidence workflow verified in browser on clean `localhost` storage: Admin Billing opened `Record Payment Evidence` for `INV-SEED-001`, showed the local-only payment evidence warning, accepted `EFT reference LOCAL-PAY-001 matched by Admin`, changed the invoice to `Paid`, showed the evidence on Admin, Client Billing Contact, and Client Operational Contact billing views, and recorded `Invoice updated` with the payment evidence in the APP-PRV-004 hash-verified audit trail.
- Policy #24 month-end financial reconciliation workflow verified in browser on clean `127.0.0.1` storage: Admin Billing showed `Policy #24 Month-End Financial Controls` for `2026-05 Revenue Control`, due `5 June 2026`, with invoice, GST, total, paid/unpaid, and overdue status. Saving without no-off-system-revenue confirmation showed the in-app `Admin Workflow Rule`; after confirming no off-system revenue and recording a reconciliation note, the period showed `Completed`, retained until `18 June 2033`, and kept `External accountant not named` visible. Retention showed 1 financial record under `Financial Reconciliation Records`, and Audit showed a hash-verified `Financial reconciliation recorded` APP-PRV-004 row. The browser was reset back to clean seeded `/booking`.
- Access governance workflow verified in browser on clean `localhost` storage: Admin Access showed 6 active login roles, 0 revoked roles, 2 staff reviews due, and Receiver as `ACT-INT-003` no-login; Admin revoked Gold Coast Cycles Billing access with reason `Role change test revoke billing access`; Billing login for `accounts@goldcoastcycles.example` was blocked before code issue; Admin restored the same role with reason `Access restored after review test`; Billing Contact login then opened the billing portal again; Admin Audit recorded `Access role revoked`, `Login blocked by revoked access`, and `Access role restored` as verified `access_control` PII actions. Structured review type smoke verified in browser: Admin opened Driver access review for Damo Reeves, selected `Role Change`, recorded `Role change review smoke test retained driver access`, saw the access row update to `Review type: Role Change`, and confirmed Audit contained `Access role reviewed` with hash verification.
- Supplier master-data monitoring workflow verified in browser on fresh `localhost` storage: Admin Suppliers showed 6 active suppliers, 6 review flags, and 0 open queue items because named dock contacts remain unresolved and no supplier-level review interval was confirmed; `Queue All Current Flags` created 6 `Supplier Master Data Review` exceptions; the investigation modal for `sup-whites` used `SOP-MDM-01 / CAP-MCL-001 / POL-MCL-001-001`, showed the supplier record instead of POD proof evidence, and accepted an Admin investigation note; after closing one supplier review exception, the Admin nav showed `Exceptions (5)`, proving the remaining supplier exceptions stayed open.
- CAP-MCL-001 Supplier Approval Gate browser smoke passed on clean `/booking` state: Admin Suppliers showed 6 active suppliers, 0 approval gaps, 6 named contact gaps, 6 review flags, `POL-MCL-001-001 Supplier Approval Gate`, and the seed approval evidence `CAP-MCL-001 v1.0 current approved supplier network`. Adding `Browser Approval Gate Test` without approval evidence was blocked with the in-app `POL-MCL-001-001 Supplier Approval Gate requires dock access, packaging standards, pickup window, and written approval evidence` rule. Completing dock-access, packaging-standards, pickup-window, and written evidence saved the supplier with 0 approval gaps and 7 named contact gaps. Queueing its review created a `Supplier Master Data Review` exception with `SOP-MDM-01 / CAP-MCL-001 review` and `named dock contact unresolved`; the browser was reset back to clean seeded `/booking`.
- During supplier monitoring verification, duplicate exception IDs were found when multiple exceptions were created in the same millisecond; local exception IDs were updated to include a random suffix and the close workflow was retested successfully.
- Driver structured outcome workflow verified in browser: Driver opened `Failed Delivery - MC-001`, entered reason `Receiver unavailable at workshop after arrival`, added handling note `Goods retained with driver; called operational contact once`, and the stop left the delivery list; Admin dashboard then showed `Failed Delivery - MC-001` in APP-ADM-005 with the captured note. Admin assigned pending `MC-002` to Damo with vehicle `MCO-OUTCOME`; Driver opened `No Pickup - MC-002`, entered reason `Supplier dock advised order was not ready for collection`, added handling note `Dock contact asked Admin to confirm follow-up`, and the stop left the pickup list; Admin dashboard then showed `Exceptions (2)` with both No Pickup and Failed Delivery exceptions and both driver notes.
- Superseded Bring Forward note: an earlier browser check verified an incorrect Bring Forward flow for `MC-002`, but direct review of `SOP-RUN-04-BringForwardPickup.xlsx` showed that behavior was wrong. The runtime has been corrected so Bring Forward means collecting a future pickup early from a supplier already on today's route, with no unscheduled detour, item/quantity capture, and the original intended delivery run date retained.
- SOP-RUN-04 Bring Forward is source/typecheck/build verified: Driver supplier stops now show `SOP-RUN-04 Future Pickups Ready Today` only for eligible future unassigned pickups at a supplier already on the current route; current-run pickup rows no longer offer Bring Forward as a postponement action; local seed `MC-005` exercises the future-pickup case. Supabase migration `202606190014_bring_forward_sop_run04.sql` adds matching collected-date, intended-run-date, no-detour, and acceptance-standard guardrails.
- Admin structured supplier/pricing action workflow verified in browser: Admin opened `Mark Supplier Reviewed` for Link International, entered `Supplier review reason retained in APP-PRV-004 audit`, and Audit showed `Supplier updated`, `Hash Verified`, and the supplier reason in the APP-PRV-004 trail. Admin opened `Archive Pricing Rule - 1 tyre`, entered reason `Local verification of pricing archive workflow` and Owner approval `OWNER-APPROVAL-LOCAL-001`, and the rule moved to Archived with a local change-log ID; Admin then opened `Reactivate Pricing Rule - 1 tyre`, entered reason `Reactivate after local modal verification` and Owner approval `OWNER-APPROVAL-LOCAL-002`, and Audit showed `Pricing rule updated`, `Hash Verified`, reason, Owner approval, and pricing master-data rows for status/change-log changes.
- Local invoice dispatch evidence workflow verified in browser: Admin selected the Admin role, logged in with a generated local testing code, opened Billing, and recorded dispatch evidence for overdue `INV-SEED-001`; the invoice remained `Overdue`, `Record Dispatch` disappeared, the Admin billing card showed `Dispatch recorded 18 June 2026: local record only; recipient accounts@goldcoastcycles.example; external status provider_not_configured`, the invoice preview showed the detailed dispatch note, and Audit showed `Invoice updated` with the recipient, channel, external status, note, and verified hash-chain state.
- Authenticated Client tracking workflow verified in browser: `gc@example.com` logged in with a generated local testing code, opened `Tracking (2)`, saw only Gold Coast Cycles records, searched by con note `A1-3311`, saw `MC-003` with delivery date from POD proof, receiver `Jake T`, private `delivery-proof/deliveries/{delivery_id}/signature.png` path, and Policy #5 retain-until date `13 May 2033`, raised `Tracking proof query from client portal` from the tracking card, and Admin then saw `Delivery Dispute - MC-003` in the APP-ADM-005 dashboard/exception queue with one linked POD proof record; Audit showed `Client dispute raised` and `Exception recorded` as verified chain events.
- Driver run-close confirmation/action-items workflow verified in browser from `UJ-DRV-001` S5: Damo logged in with a generated local testing code, recorded `MC-006` as No Pickup, used SOP-DEL-04 `Report Price Discrepancy` for the grouped `MC-001`/`MC-004` delivery, opened Run Close with `0` open stops, `2` delivered, `1` no pickup, `2` failed delivery, and `4` open exceptions, then closed the run. Driver saw `Run complete. Good work.`, summary stats, logout, and retained-goods action items for `MC-001` and `MC-004`. Admin then opened `Run Close (1)` and saw `Run Close Review`, the same no-pickup/failed-delivery counts, linked open exceptions, and `Driver close action items` for both work items. The browser was reset to `/booking` with no console errors. Supabase migration `202606190031_run_close_confirmation_uj_drv001.sql` adds matching run-close summary/action-item fields and retained-goods reconciliation guardrails.
- Local operational notice/outbox workflow verified in browser: Client Operational Contact `gc@example.com` created pickup `MC-63125` with con note `NOTICE-001` after the 12:30pm Brisbane cut-off; Client `Updates (2)` showed `Pickup request received - MC-63125` and `Run date adjusted - MC-63125`, both `LOCAL` with `provider_not_configured` and Track Order links. Admin then saw `Updates (2)` and APP-PRV-004 audit events for both local notice records. Admin dispatched `MC-63125` to Damo with vehicle `MCO-NOTICE`; Driver confirmed pickup from `price_rules` and started delivery; Admin then saw `Updates (4)` with added `Pickup confirmed - MC-63125` and `Out for delivery - MC-63125`, all retained as local records with `provider_not_configured`.
- Admin pricing governance monitor verified in browser: Admin Pricing showed 9 active rules, 0 review flags, 0 open queue items, `Policy #9`, `SOP-MDM-02`, and `Pricing Governance Monitor`; adding `Verification governance rule` with a written change reason and Owner approval reference created a generated local change-log ID and left the monitor clean.
- Account activation outbox workflow verified in browser: a public registration for `ACTIVATION NOTICE WORKSHOP` created a Pending account; Admin completed the B2B, physical SEQ address, contact, and supplier eligibility review; Admin Updates increased to 5 and showed `Account activated - ACTIVATION NOTICE WORKSHOP`, `Account`, `Account activated`, and `provider_not_configured`; the Client Operational Contact logged in, completed first-login supplier setup, saw `Updates (1)`, and the same account-level activation notice appeared without a Track Order action.
- APP-ADM-002 local run compiler verified in browser after fleet-register replacement: Admin Dispatch showed `MC-002` eligible for run `14 May 2026`, previewed supplier `Ficeda` and delivery zone `Coolangatta`, required the controlled `MCO-001 - Damo Reeves` vehicle record, compiled the run so Dispatch dropped to 0, and Assigned Run showed `RUN-2026-05-14-d1-MCO-001`, sequence `1`, zone `Coolangatta`, and fleet register compliance evidence. Driver login then showed `Compiled Run Brief`, vehicle `MCO-001`, supplier `Ficeda`, `Seq 1`, `Coolangatta`, and `Fleet compliance attested`.
- CAP-MCL-002 Run Planning Monitor is source/typecheck/build/browser verified: fresh Admin login opened Dispatch, showed compilation rate, named assignment rate, fleet gate pass rate, Admin intervention count, `POL-MCL-002-001`, night-before compile due date, unassigned stop count, and `Queue APP-ADM-002 Exception` actions. `Queue All Run Planning Exceptions` created two APP-ADM-005 `Run Planning Exception` rows, raised Exceptions from 1 to 3, disabled the queued row buttons, and the Exception Queue showed both CAP-MCL-002 / APP-ADM-002 exception notes with night-before compile due evidence. Supabase migration `202606190015_run_planning_monitor_cap_mcl002.sql` adds expected compile date, compiled-night-before evidence, Admin intervention reason, and linked run-planning exception fields.
- Fleet register workflow verified in browser: Admin Fleet showed 2 vehicle records, 1 active dispatch-ready `MCO-001`, 1 blocked `ACT-VEH-002` with unconfirmed details, APP-FLT-001 local register text, Admin vehicle fields for insurance policy/reference, GVM, make/model/year, last reviewed date, service dates, defect status, and change reason. Admin Dispatch used the controlled `MCO-001 - Damo Reeves` vehicle selector, blocked free-text vehicle entry, recompiled `MC-002` through the vehicle record, and Driver run brief then showed `Vehicle MCO-001` with `Fleet compliance attested`.
- POD proof path workflow verified in browser: Admin Retention and Client Tracking both showed the local proof object as `delivery-proof/deliveries/{delivery_id}/signature.png`, no longer a generic private-bucket placeholder, while retaining Policy #5 retain-until evidence and destruction blocked pending Privacy Owner approval.
- Non-payment suspension gate verified in browser: Admin Billing showed `INV-SEED-001` with `Notice Required` disabled before Day 8 notice evidence, `Record Day 8 Notice` reduced the notice queue to 0 and enabled `Suspend Account`, and the suspension modal showed both `Operational contact notified` and `Billing contact notified` evidence controls.
- Workflow-rule notices hardened and verified: the runtime verifier now checks the Driver, Admin, and root System notice titles plus their source-backed guard strings. Browser verification on fresh local seed confirmed Driver POD completion cannot proceed without receiver/signature evidence and shows `Driver Workflow Rule` with `Receiver name and signature required`. Admin Orders row `MC-001` was opened, the status control was changed to `Delivered`, and the app kept the order `En Route` while showing `Admin Workflow Rule` with the SOP-DEL-05 proof-driven delivery message. The root `System Workflow Rule` remains source-verified; normal UI paths currently hide or disable the invalid actions behind those root handlers before a browser-clickable trigger is available.
- Supabase environment guard verified with direct runtime cases: preview app plus production-labelled Supabase is blocked; preview app plus preview-labelled Supabase is allowed; missing local Supabase credentials return a non-live status instead of creating a client.
- Route-aware entry verified in browser before login: `/admin` showed Admin active and Admin operations copy, `/driver` showed Driver active and Driver run copy, `/tracking` showed Client active and Tracking copy, `/booking` showed Client active and Booking copy, and `/portal` showed Client active and Client portal copy. Later authenticated browser checks successfully used generated local testing-code login for Admin, Client Billing Contact, and Client Operational Contact.
- Local demo reset control verified in browser: `/login` rendered `Get Login Code` and `Reset Local Demo Data`, reset opened the in-app `Confirm Reset` / `Keep Demo Data` confirmation panel without clearing current data, and `Keep Demo Data` returned to the login controls. This gives the active shell a visible local-only path to clear Moto & Co session-storage demo state and reload seeded workflows such as `MC-UNMATCHED-001`.
- Account suspension notice workflow verified in browser: Admin logged in with a generated local testing code, opened Billing, used the Day 8 notice-backed `Suspend Account` action for `INV-SEED-001`, confirmed the exact account name and both contact-notification evidence, and Admin Updates showed `Account suspended - Gold Coast Cycles` with `Account suspended` type and `provider_not_configured`.
- Client Billing Contact account-notice workflow verified in browser: `accounts@goldcoastcycles.example` logged in with a generated local testing code, saw account status `Suspended`, saw both `Account suspension notice` and `Day 8 overdue notice` as separate local records, and still had no pickup, order, or supplier controls.
- Client Operational Contact account-notice workflow verified in browser: `gc@example.com` logged in with a generated local testing code, saw `+ New Pickup` disabled by `Account Suspended`, saw the `Account suspended - Gold Coast Cycles` operational update with `provider_not_configured`, and saw the `Account suspension notice` label in the Billing tab.
- Client Billing Contact billing-query acknowledgement verified in browser on fresh `localhost` storage: `accounts@goldcoastcycles.example` logged in with a generated local testing code, raised a query for `INV-SEED-001`, and the Billing portal showed `Billing Updates`, `Billing query received`, `Local record only`, `provider_not_configured`, and the submitted query note.
- Billing-query status/investigation workflow verified in browser: Client Billing Contact saw `Billing Query Status` for `INV-SEED-001` with the open Admin exception; Admin opened APP-ADM-005 Exception Queue, reviewed the linked invoice, delivered work, receiver, and POD proof record, closed the Billing Dispute with Policy #18 / APP-DRV-003 investigation evidence, and Billing Contact then saw `Closed` plus the Admin investigation outcome in the billing-only portal.
- Policy #14 order cancellation workflow verified in browser on `localhost`: Client Operational Contact created `MC-68393` for run `23 June`, saw `Cancel Order`, cancelled it before cut-off, saw `Order cancelled - MC-68393` with `provider_not_configured`, and APP-PRV-004 showed hash-verified cancellation audit rows. Moto Madness Client Operational Contact saw seeded post-cut-off pending `MC-002` with `Request Cancellation Review`, submitted a note, Admin saw `Cancellation Request - MC-002` in APP-ADM-005, used `Accept Cancellation`, and the exception closed with linked work `MC-002 Cancelled`, dispatch dropping to zero, local `order_cancelled`/`cancellation_requested` notices, and hash-verified audit rows.
- Policy #8 failed-delivery/redelivery-fee workflow verified in browser on `localhost`: Driver recorded failed delivery attempt 1 for `MC-001` with reason and handling note; Admin saw `Attempt 1 of 2`, `Goods retained with driver`, and `redelivery fee Not Applicable`, then used `Authorise Second Attempt`; Driver recorded attempt 2; Admin saw `Pending Admin Review ($10.00)`, opened `Approve Redelivery Fee`, confirmed the required review note, and the exception closed as `Redelivery fee approved`. Admin Billing then showed one approved `Policy #8 redelivery fee` line for `MC-001`, subtotal `$10`, GST `$1`, total `$11`; `Create Draft Invoice` produced `INV-089712` with one redelivery-fee line and the unbilled approved-fee queue dropped to 0. APP-PRV-004 audit showed hash-verified rows for both attempts, Admin authorisation, fee approval, and draft invoice creation.
- Policy #15 / Policy #16 vendor pickup standards workflow verified in browser on `localhost`: Client Operational Contact created `MC-52818` for Link International; Admin compiled it into `RUN-2026-06-23-d1-MCO-001` for Damo with controlled vehicle `MCO-001`; Driver saw the assigned supplier pickup, was blocked from pickup capture until pre-trip checks were complete, opened pickup capture with ready/labelled/packaged/grace/compliance-note evidence fields, then recorded `No Pickup` with category `Unlabelled item`, grace `0`, reason, and driver handling note. Admin dashboard and APP-ADM-005 showed `No Pickup - MC-52818` with Policy #15 / Policy #16 category and no-billable-row evidence. Admin Suppliers showed Pickup Standards Monitor with Link International flagged at 50% No Pickup, 1 packaging/label refusal, queued `Supplier Pickup Standards Review`, and the investigation modal showed Policy #15 / Policy #16 / CAP-MCL-001 / APP-SRM-001a source, supplier record, 2 pickup records, 1 No Pickup, 50% rate, and 1 packaging refusal. A daily alert label bug found during this verification was patched so supplier pickup standards show `Policy #15 / Policy #16 / CAP-MCL-001 review` rather than the supplier master-data label.
- SOP-DEL-01 grouped delivery stop workflow verified in browser on `localhost`: fresh local demo data showed Driver run brief with 2 open work items but Delivery with 1 grouped stop for Gold Coast Cycles at `22 Ferry Rd, Southport QLD`; the stop contained `MC-001` and `MC-004`, both suppliers, both con notes, pickup-captured item/price evidence, and stop total `$55.00`. The grouped sign-off modal showed one receiver/signature scope and read-only APP-DRV-002 pickup prices; blank completion showed `Driver Workflow Rule` / `Receiver name and signature required`; entering `Browser SOP DEL01 Receiver` and drawing one signature completed both work items. Driver Delivery dropped to 0 and Completed rose to 4; Admin Billing showed `MC-001` and `MC-004` as the 2 Gold Coast Cycles `SOP-DEL-05 billing-ready` lines; Admin Audit showed one `Delivery proof stored` row with group size 2 and one `Delivery completed by system` row listing `MC-001, MC-004` under `SOP-DEL-01 / SOP-DEL-05`, with hash verification.
- SOP-PUP-03 No Pickup reason taxonomy is source/typecheck/build verified: the Driver No Pickup select now includes goods not ready after 10-minute grace, unlabelled or con-note mismatch, improper packaging, supplier refused pickup, and wrong items presented. Supabase migration `202606190013_no_pickup_reason_taxonomy_sop_pup03.sql` extends the `pickup_requests` reason constraint without finalising unconfirmed Bring Forward, Failed Delivery, offline retry, retained-goods, or wrong-address reason codes.
- SOP-PUP-02 supplier-stop closeout is source/typecheck/migration/browser verified: Driver supplier stops remain open after individual customer outcomes, `Close Supplier Stop` is blocked until all customers have Picked Up, No Pickup, or Brought Forward outcome evidence, closeout captures correct dock, customer-list review, no-ad-hoc-record, dock-contact engagement, leave-dock confirmation, outcome summary, driver/timestamp, and optional note, and delivery start is blocked until supplier stops are closed. Browser verification on clean seeded data used local fixture `MC-006`: Driver saw `Pickup (1)`, confirmed incomplete closeout was blocked, completed pre-trip, captured `MC-006` as Picked Up with Policy #15/#16 evidence, confirmed delivery start was blocked before supplier-stop closeout, recorded all SOP-PUP-02 closeout checks plus note, saw `Pickup (0)`, then started grouped delivery and confirmed `MC-006` moved to `EN ROUTE`. The browser was reset to `/booking` with no console errors. Supabase migration `202606190029_supplier_stop_closeout_sop_pup02.sql` adds matching closeout fields and guardrails. SOP-PUP-02's `Time constraint` wording is not added as a No Pickup category because SOP-PUP-03's confirmed reason taxonomy does not include it.
- SOP-DEL-04 delivery sign-off proof is source/typecheck/migration/build/browser verified: Driver grouped delivery sign-off now gates POD capture behind physical-address match, goods-match, authorised-receiver, handover, and read-only-price confirmations; receiver full name cannot be a generic placeholder; signature capture also requires driver device-supervision confirmation. Browser verification on clean seeded data opened the Gold Coast Cycles grouped delivery stop for `MC-001` and `MC-004`, confirmed Failed Delivery is blocked without a SOP-DEL-04 category, confirmed `Next` is blocked until the address confirmation is checked, completed all five pre-POD confirmations, entered receiver `Browser SOP DEL04 Receiver`, drew a signature, confirmed completion is blocked until `Driver supervised signature capture and kept the device in sight` is checked, then completed the grouped stop. Driver Delivery dropped to `0`, Completed rose to `4`, and `MC-001` plus `MC-004` both showed `Received by: Browser SOP DEL04 Receiver`. The browser was reset to `/booking` with no console errors. Supabase migration `202606190030_delivery_signoff_sop_del04.sql` adds matching proof sign-off fields, sign-off constraint, failed-delivery category columns, and category constraint.
- SOP-DEL-04 price-discrepancy sign-off path is source/typecheck/browser verified: Driver opened the Gold Coast Cycles grouped delivery stop for `MC-001` and `MC-004`, clicked `Report Price Discrepancy` from the read-only price review section, and the Failed Delivery evidence modal opened preloaded with category `Price appears incorrect`, reason `Read-only delivery price appears incorrect; driver did not complete delivery.`, stop total `$55.00`, and both item-price lines. Recording the outcome did not complete the delivery: Driver Delivery dropped to `0`, Completed stayed `2`, and Admin Dashboard/APP-ADM-005 showed open Failed Delivery exceptions for both grouped work items with SOP-DEL-04 price-discrepancy evidence and no POD proof. The browser was reset to `/booking` with no console errors.
- Policy #15 goods acceptance workflow is source/typecheck/build/browser verified: after fresh reset, Admin compiled `MC-002` into Damo's `MCO-001` run; Driver completed pre-trip checks and opened `Pickup Items - MC-002`; the modal showed `Policy #15 / Policy #16 pickup standards`, the tyre-specific standard `Tyres upright or stacked, secured, not loose, not flat or rolling freely`, and the no-under-protest final decision checkbox. Confirming pickup with only the Policy #16 ready/labelled/packaged checks was blocked by `Policy #15 requires item-specific goods acceptance`; confirming after item acceptance but without final dock decision was blocked by the under-protest rule. Completing both Policy #15 checks and a compliance note accepted the pickup, cleared `Pickup (0)`, moved `MC-002` to Delivery, and the browser was reset to clean `/booking`. Supabase migration `202606190017_goods_acceptance_policy15.sql` adds matching goods-acceptance confirmation, final-decision, refused-goods, and policy-reference fields.
- SOP-RUN-04 source-correct Bring Forward workflow was source/typecheck/build/browser smoke verified: after a fresh reset and Driver login, the Driver dashboard showed `SOP-RUN-04 Future Pickups Ready Today - Link International` for `MC-005`, with intended run `19 May`, current route `14 May`, no-detour wording, and `Record Brought Forward Pickup`. The modal showed reason, item type, quantity, collected current-route date, intended delivery run date, calculated pickup price, and required no-detour/labelling/packaging evidence. Blank submit showed `Driver Workflow Rule` / `Outcome reason is required.`; completing reason plus all three evidence checks accepted the capture and removed the future-pickup candidate. A live Driver render-order bug found during this smoke (`activeRunDates` initialisation order) was patched, and `npm.cmd run verify:local` passed afterward. The browser was reset back to clean seeded `/booking` state.
- SOP-DEL-05 delivery completion was source/typecheck/build/browser verified: Driver completion no longer calls the order update path directly, `addDeliveryProof` stores immutable proof then system-completes the linked order as Delivered, marks it billing-ready, writes the delivered operational notice, and writes `Delivery completed by system` audit evidence. Admin order status shortcuts now block manual Delivered writes or Delivered reversals, and Admin Billing now requires proof-linked Delivered work before invoice compilation. Fresh browser verification on `localhost` reset local demo data, logged in as Driver, completed the visible pre-trip checks, opened `MC-001`, confirmed missing POD evidence shows the in-app `Receiver name and signature required` rule, selected `2 tyres - $40.00`, entered receiver `Browser SOP DEL05 Receiver`, drew a signature, and completed delivery. Driver Completed showed `MC-001` as Delivered with that receiver; Admin Dashboard showed 3 Delivered / 0 En Route; Admin Audit showed `Delivery proof stored`, `Delivery completed by system`, `billing-ready under SOP-DEL-05`, and hash verification; Admin Billing showed `MC-001` as `SOP-DEL-05 billing-ready`.
- Policy #18 delivery dispute and billing-query workflow is source/typecheck/build/browser verified: Client Operational delivery disputes require dispute type, delivery date in question, and description; Client Operational/Billing Contact incorrect-charge queries require invoice line/order reference, delivery date in question, and description. Billing-query delivery date now prefills from the selected invoice line's linked work item while staying editable/required. Fresh browser verification on `localhost` reset local demo data, raised a Client Operational delivery dispute for `MC-003`, confirmed the missing description guard, saw local acknowledgement with Admin acknowledgement and resolution targets, raised a Billing Contact query for `INV-SEED-001`, confirmed date prefill `2026-05-13`, confirmed the missing description guard, and saw Billing Query Status. Admin APP-ADM-005 showed both Policy #18 exceptions with invoice timing, ack due `22 June 2026`, resolution due `2 July 2026`, linked invoice/work/POD evidence, and Owner escalation state; Admin acknowledged and escalated the billing dispute, closed it with `Billing error confirmed - credit note required`, and recorded a local credit-note obligation without claiming external issue/export. Admin also closed the delivery dispute after reviewing POD proof `proof-seed-1`. Billing Contact saw the closed query, Admin investigation, and credit-note-required obligation; Client Tracking showed the delivery dispute outcome; Client Billing showed the billing dispute outcome/remedy line; APP-PRV-004 audit showed dispute raised, acknowledgement, Owner escalation, closure, outcome recording, and hash verification. Supabase migration `202606190007_policy18_dispute_sla.sql` adds matching dispute metadata, remedy fields, work/invoice outcome-history fields, and guardrails.
- Day 8 overdue local generation was source/typecheck/build/browser verified: the local runtime creates system-generated billing notice evidence for overdue invoices once the due date plus 8 days has passed, labels the record source in Admin, Client Operational, and Billing Contact billing views, keeps Admin fallback recording for missing notices, and leaves external delivery as `provider_not_configured`. Fresh-seed browser verification opened Admin Billing and confirmed `INV-SEED-001` showed `Day 8 overdue notice recorded 18 June 2026; source system generated; channel local record only`, while the Day 8 fallback queue showed 0 invoices and `No overdue invoices awaiting a Day 8 notice.` Supabase migration `202606190008_day8_overdue_notice_generation.sql` adds matching generation fields, a callable local-evidence generator, and an invoice-change trigger. Production daily scheduling and outbound delivery remain open until the live platform and notification channel are confirmed.
- SOP-EXC-03 unmatched billing account workflow is source/typecheck/build/browser verified: Admin Billing excludes unmatched billing candidates from invoice batches, shows an Unmatched Billing Account queue with work/proof/address/candidate-account evidence, queues APP-ADM-005 exceptions, and lets Admin correct account_id only to an active account with an investigation note. Fresh browser verification on `localhost` reset local demo data, opened Admin Billing, confirmed `MC-UNMATCHED-001` was blocked from billing with proof `proof-seed-unmatched-1`, opened `Correct Billing Account`, confirmed blank save shows `Admin investigation note required before correcting an unmatched billing account.`, recorded POD/con-note/address/run-date evidence, matched the work to Gold Coast Cycles, saw the unmatched queue clear, saw `MC-UNMATCHED-001` move into Delivered Work And Approved Fees, confirmed the APP-ADM-005 exception closed as `Billing account match corrected`, confirmed APP-PRV-004 audit rows for unmatched-billing queueing, order update, exception close, and hash verification, and created a draft invoice from the corrected work to prove the billing exclusion was lifted. Supabase migration `202606190009_unmatched_billing_account_exceptions.sql` adds matching pickup-request match fields, exception queueing, and an Admin-only correction function that blocks retro-modifying already-invoiced work.
- SOP-BIL-04 invoice approval gate was source/typecheck/build/browser verified: newly created draft invoices record billing-group approval evidence, Admin must approve the rendered invoice with a review note before dispatch, dispatch moves Approved/Draft invoices to Sent with local evidence only, and payment/overdue controls require dispatch evidence. Supabase migration `202606190010_invoice_approval_gate_sop_bil04.sql` adds matching approval fields, an Admin-only approval function, and status guardrails. Browser smoke check confirmed seeded `INV-SEED-001` shows rendered-invoice approval plus dispatch evidence and no longer exposes pre-approval dispatch. Fresh browser exercise after Driver POD completion created `INV-107054` from `MC-001`, verified dispatch was blocked until approval, approved the rendered invoice with Admin review note `Browser SOP-BIL-04 approval: MC-001 proof-linked line and billing contact checked.`, recorded local dispatch evidence `Browser SOP-BIL-04 dispatch evidence: approved invoice retained locally; provider_not_configured.`, moved the invoice to `Sent`, and confirmed APP-PRV-004 audit rows for draft creation, approval, dispatch, and hash verification.
- HCM extraction was completed after product-boundary correction: APP-ADM-006/SOP-JDD driver legal record governance, Policy #13 verification, Policy #19 driver conduct/discipline material, and Policy #25/#26 driver/courier expansion gates were removed from the active logistics runtime and active Supabase migration set. The draft runtime and migrations are preserved under `hcm-extract/` for a future HCM app.
- Policy #20 AI use governance is source/typecheck/migration/browser-smoke verified: Admin `AI Drafts` shows the three confirmed agents (`AGT-CS-001b`, `AGT-SRM-001b`, `AGT-ADM-007b`), source-backed controls for Admin trigger only/no autonomous send/no batch approval/provider not configured, and live candidates from CRM, supplier, and APP-ADM-005 exception monitors. Browser verification on `127.0.0.1` logged in as Admin, opened `AI Drafts`, confirmed 9 flagged candidates, created a Gold Coast Cycles Customer Success draft, saw Pending Review increase to 1, saw duplicate unread draft creation disabled as `Unread Draft Exists`, confirmed approval is blocked without an Admin review note, approved with review evidence, and confirmed the final state `Approved - Not Sent` with `not_sent_provider_not_configured`. Admin Audit showed hash-verified `Policy #20 AI draft requested` and `Policy #20 AI draft reviewed` rows. The local demo data was reset and the browser was returned to `/booking`.
- Policy #6 NDB response is source/typecheck/migration/browser-smoke verified: Admin `NDB` showed the `Policy #6 / POL-OPS-006 NDB Response Register`, suspected incident intake fields, 30-day assessment due date, Privacy Owner unnamed blocker, APP-PRV-004 audit-ref evidence, OAIC/affected-person evidence fields, and post-breach review report retention surface. Browser verification on `127.0.0.1` logged in as Admin, confirmed blank incident save is blocked, recorded a suspected POD-proof access incident with containment and APP-PRV-004 refs, saw `NDB (1)`, Open Incidents `1`, Owner Blocked `1`, and `Blocked - Privacy Owner Unnamed`, clicked `Eligibility Decision Blocked` and confirmed Admin/automation cannot make the eligible-breach decision, confirmed Admin Audit showed hash-verified `Policy #6 NDB incident recorded` with protected object `privacy_incident`, and confirmed Retention shows `Policy #6 Post-Breach Review Reports` without inventing a report row. The local demo data was reset and the browser was returned to `/booking`.
- Policy #21 / Policy #7 data-use governance is source/typecheck/migration/browser-smoke verified: Admin `Data Use` records operational access, data export, Digiverse production access, third-party sharing, marketing use, and data-access breach escalation. Browser verification on clean `127.0.0.1` state opened Admin `Data Use`, confirmed blank save is blocked, recorded `browser export request missing approval` as `Blocked` with `Policy #21 requires Admin approval evidence before data export.`, then recorded `digiverse support access review` as `Approved` with `DIGI-ACCESS-001` production access log and `ADMIN-DATA-USE-001` Admin evidence. The tab stayed `Data Use (1)` because only the export was blocked; APP-PRV-004 Audit showed `Hash Verified`, `Policy #21 data use recorded`, protected object `data_use`, and both record details. The local demo data was reset and the browser returned to `/booking` with no console errors. Supabase migration `202606190026_data_use_policy21_policy7.sql` adds Admin-only RLS, blocked-reason capture, guardrail checks, and PII audit.
- Policy #3 / Policy #4 / Policy #5 privacy request governance is source/typecheck/migration/browser-smoke verified: Admin `Privacy` records APP 12 access requests, APP 13 correction requests, privacy complaints, and APP 4 unsolicited-information assessments. Browser verification on clean `127.0.0.1` state opened Admin `Privacy`, confirmed blank save is blocked, recorded `Browser Privacy Requester` as an `Unsolicited Information` APP 4 assessment with destruction/de-identification requested and no Privacy Owner evidence, and saw it stored as `Blocked - Privacy Owner Required` with the Policy #5 destruction block. It then recorded `Gold Coast Cycles Ops` as a resolved APP 12 access request with `APP12-ACCESS-LOCAL-001` response evidence. APP-PRV-004 Audit showed `Hash Verified`, `Policy #3 privacy request recorded`, protected object `privacy_request`, and both record details. The local demo data was reset and the browser returned to `/booking` with no console errors. Supabase migration `202606190027_privacy_requests_policy3_policy4_policy5.sql` adds Admin-only RLS, 30-day response due dates, 5-business-day complaint acknowledgement due dates, refusal/access/correction/APP 4 evidence checks, Privacy Owner destruction blocking, and PII audit.
- Policy #23 account suspension and termination is source/typecheck/migration/browser-smoke verified: Admin Clients now uses a controlled suspension ground, requires Day 8 notice evidence for non-payment suspension, requires Admin notice plus unremedied-breach evidence for conduct suspension, and keeps both-contact notification evidence for suspension/reinstatement. Browser verification on clean `127.0.0.1` state opened Admin Clients, used Moto Madness to confirm blank conduct suspension is blocked with `Conduct breach notice evidence required before suspension.`, completed conduct suspension with Admin notice/unremedied-breach evidence, opened termination, selected `Repeated non-payment`, and confirmed it is blocked by the unresolved debt recovery escalation/write-off threshold gap. It then completed valid conduct termination with prior conduct suspension, Owner consultation evidence, written termination notice evidence, and exact account-name confirmation. Admin Updates showed `Account terminated - Moto Madness` with `provider_not_configured`, Admin Audit showed `Hash Verified`, `Account termination notice recorded`, `Operational notice recorded` for `account_terminated`, and `Customer account updated` with termination evidence. The local demo data was reset and the browser returned to `/booking` with no console errors. Supabase migration `202606190028_account_termination_policy23.sql` adds `account_terminations`, account termination notice types, Owner/written-notice guardrails, and a trigger that blocks repeated non-payment termination while the source gap remains open.
- Admin Billing route smoke check verified in browser: `/admin` opened the Admin local testing-code login, Admin workspace loaded, Billing rendered, and the `Unmatched Billing Account Queue` plus SOP-EXC-03 text appeared. Fresh seed data now includes `MC-UNMATCHED-001` / `proof-seed-unmatched-1` as a purpose-built SOP-EXC-03 unmatched-account fixture for the correction-modal path; existing browser session state must be reset before that seed fixture appears.
- BOAS Sheet 05 and Sheet 06 were inspected from `MotoCo_Unified_BOAS_Hierarchy_v1.6.xlsx`; the first-pass RLS migration was statically checked against current migration table/column names. It has not been executed because `psql`, Supabase CLI, Docker, and live Supabase credentials are unavailable locally.

Build result:

- Local preflight passed with `NEXT_DIST_DIR=.next-preflight-build-assignmentfixture npm.cmd run verify:local`.
- Runtime requirement verification passed with 81 source-backed checks.
- Platform environment contract report passed locally; GitHub, Supabase live project, and Vercel values remain open.
- Launch readiness report passed locally as a read-only report; Git CLI, Supabase CLI, Vercel CLI, GitHub values, Supabase values, Vercel values, and local Git repository initialisation remain open.
- Strict production readiness gate failed as expected with `npm.cmd run verify:production`; it reported missing GitHub values, Supabase values, Vercel values, Git CLI, Supabase CLI, Vercel CLI, and local Git repository initialisation.
- Supabase migration guardrail verification passed with 37 source-backed checks.
- Typecheck passed.
- Production build passed. Latest full preflight used isolated `NEXT_DIST_DIR=.next-preflight-build-assignmentfixture`; final post-preflight `typecheck`, `verify:requirements`, and `verify:migrations` also passed.
- Public website route verified in browser: `/website` rendered the approved PNG logo, confirmed `Brisbane suppliers to Gold Coast workshops` geography, operating cadence, explicit black-and-white photo placeholders, `Book a pickup`, `Track a delivery`, `Open the logistics app`, and `View legal library status` links. The legal link opened `/legal` with 8 legal rows and 8 `Not Published` statuses; booking and tracking links opened `/booking` and `/tracking` active login contexts with `Get Login Code`; the app link opened `/` with Client/Billing/Driver/Admin role entry and `Reset Local Demo Data`. Mobile viewport check at 390px showed no horizontal overflow and all public links remained available.
- Browser smoke check passed after the single-runtime cleanup and reset-confirmation update: `/` rendered the active login/runtime shell with `Get Login Code` and `Reset Local Demo Data`, no old placeholder login/invoice copy was present, `/login` opened and dismissed the in-app reset confirmation without clearing data, `/legal` rendered 8 legal rows with 8 `Not Published` status badges, and `/booking` was restored with the Booking route context visible.
- Driver SOP-RUN-04 browser smoke passed after the source-correct Bring Forward rewrite and render-order patch: `/driver` showed the future pickup action, modal guard, accepted evidence capture, and clean reset back to `/booking`.
- Admin CAP-MCL-002 browser smoke passed: `/admin` Dispatch rendered the Run Planning Monitor, queued APP-ADM-002 planning exceptions into APP-ADM-005, and reset back to clean `/booking`.
- Draft GitHub Actions CI file is present locally but has not run in GitHub because the repository is not connected.
- Supabase RLS/storage migration execution is not locally proven; live Supabase migration and Storage policy testing remain required.
- Authenticated account-notice browser checks passed for Admin, Client Billing Contact, and Client Operational Contact; typecheck, production build, source scans, and pre-login route checks passed.
- Browser dev logs retained duplicate-key warnings from the pre-fix supplier queue test in the earlier `127.0.0.1` tab; the duplicate exception ID issue was fixed and verified on fresh `localhost` state.
- Development Fast Refresh warning appeared during editing only.

Audit result:

- `npm audit` reports two moderate vulnerabilities through Next/PostCSS.
- npm's suggested fix is a major downgrade to an old Next version, so no automated force fix was applied.

## Local URL

The local dev server is running at:

```text
http://127.0.0.1:3000
```

## Remaining Gaps

See `build-gaps.md`.
